# MiniAgent Data Model

## 1. Design Goals

The backend data model must make MiniAgent recoverable after process crashes, UI disconnects, Feishu rate limits, Agent context overflow, and scheduled task retries.

Core rules:

- `EventStore` is the source of truth for runtime history.
- `Outbox` is the source of truth for delivery work.
- `Session`, `AgentRun`, and `Task` are separate state machines.
- `ContextPack` is the portable context unit for compact, resume, and handoff.
- Raw history is never deleted by compact; compaction only changes what is injected into the next Agent run.

## 2. Storage Conventions

- Database: SQLite through `better-sqlite3`.
- IDs: string ULIDs or UUIDv7, generated by the application.
- Timestamps: ISO-8601 UTC text.
- JSON columns: stored as `TEXT` containing validated JSON.
- Enums: stored as `TEXT` with application-level validation, plus `CHECK` constraints where practical.
- Deletes: prefer soft-delete or terminal states. Do not physically delete history in normal flows.
- Replay cursor: `events.global_seq` is the only canonical replay cursor for Projectors, Web reconnect, and recovery scans.

## 3. Relationship Overview

```text
agent_profiles 1 ── * sessions 1 ── * agent_runs 1 ── * events
      │                  │              │
      │                  │              └── * outbox
      │                  │
      └── * agent_defaults
                         ├── * tasks
                         ├── * messages
                         ├── * context_packs
                         └── * audit_logs
```

## 4. Tables

### `agent_profiles`

Describes available runtime adapters.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | `codex`, `claude`, `trae` |
| `display_name` | TEXT | UI label |
| `command` | TEXT | Executable name or absolute path |
| `capabilities_json` | TEXT | Structured events, compact, resume, images, permissions |
| `default_args_json` | TEXT | Default CLI args |
| `health_status` | TEXT | `unknown`, `healthy`, `missing`, `auth_required`, `failed` |
| `last_probe_at` | TEXT NULL | Last probe timestamp |
| `created_at` | TEXT |  |
| `updated_at` | TEXT |  |

### `agent_defaults`

Stores default Agent resolution for `user -> channel -> workspace -> system`.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | Default rule ID |
| `scope_type` | TEXT | `user`, `channel`, `workspace`, `system` |
| `scope_ref` | TEXT | User ID, channel ID, workspace path, or `global` |
| `agent_type` | TEXT | Target `agent_profiles.id` |
| `params_json` | TEXT | Optional default runtime params |
| `created_at` | TEXT |  |
| `updated_at` | TEXT |  |

Recommended constraints:

- Unique `(scope_type, scope_ref)`.
- `system/global` must always have a valid fallback Agent.

### `sessions`

Long-lived conversation container.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | Session ID |
| `title` | TEXT | Human label |
| `agent_type` | TEXT | FK-like reference to `agent_profiles.id` |
| `workspace_path` | TEXT | Must pass `WorkspacePolicy` |
| `status` | TEXT | `idle`, `running`, `compacting`, `failed`, `archived` |
| `channel_type` | TEXT NULL | `web`, `feishu`, future channels |
| `channel_ref` | TEXT NULL | Chat/thread/user identifier |
| `default_params_json` | TEXT | Model/profile flags for this Agent |
| `active_run_id` | TEXT NULL | Current active run, if any |
| `current_context_pack_id` | TEXT NULL | Latest usable ContextPack |
| `source_session_id` | TEXT NULL | Set for handoff sessions |
| `source_context_pack_id` | TEXT NULL | ContextPack used for handoff |
| `created_at` | TEXT |  |
| `updated_at` | TEXT |  |
| `archived_at` | TEXT NULL |  |

Recommended indexes:

- `idx_sessions_status`
- `idx_sessions_channel`
- `idx_sessions_agent_type`

### `tasks`

Represents one trigger source.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | Task ID |
| `session_id` | TEXT NULL | Existing session or null before creation |
| `source_type` | TEXT | `web`, `feishu`, `cron`, `handoff`, `mcp`, `system` |
| `source_ref` | TEXT NULL | Channel event ID, schedule ID, command ID |
| `type` | TEXT | `message`, `compact`, `handoff`, `schedule_run`, `stop`, `resume` |
| `status` | TEXT | `scheduled`, `queued`, `running`, `succeeded`, `failed`, `cancelled`, `paused` |
| `target_agent_type` | TEXT NULL | Used for new session or handoff |
| `input_json` | TEXT | User input or command payload |
| `dedupe_key` | TEXT NULL | Prevents duplicate channel/web processing |
| `run_id` | TEXT NULL | Created run, if any |
| `queued_at` | TEXT NULL |  |
| `started_at` | TEXT NULL |  |
| `finished_at` | TEXT NULL |  |
| `created_at` | TEXT |  |
| `updated_at` | TEXT |  |

Recommended constraints:

- Unique nullable `dedupe_key` when present.
- `session_id` may be null only for session-creating tasks.

### `agent_runs`

Represents one Agent process execution.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | Run ID |
| `session_id` | TEXT | Parent session |
| `task_id` | TEXT NULL | Triggering task |
| `agent_type` | TEXT | Copied from session at run start |
| `status` | TEXT | `queued`, `starting`, `running`, `waiting_permission`, `compacting`, `stopping`, `succeeded`, `failed`, `cancelled`, `overflowed` |
| `launch_spec_json` | TEXT | Command, args, cwd, env summary |
| `pid` | INTEGER NULL | Local process ID while active |
| `context_pack_id` | TEXT NULL | ContextPack injected at start |
| `first_global_seq` | INTEGER NULL | First runtime event cursor |
| `last_global_seq` | INTEGER NULL | Last runtime event cursor |
| `heartbeat_at` | TEXT NULL | RuntimeSupervisor heartbeat |
| `started_at` | TEXT NULL |  |
| `stopped_at` | TEXT NULL |  |
| `exit_code` | INTEGER NULL | Process exit code |
| `stop_reason` | TEXT NULL | User stop, crash, overflow, compact, success |
| `error_class` | TEXT NULL | Adapter-classified error |
| `created_at` | TEXT |  |
| `updated_at` | TEXT |  |

Recommended partial unique index:

```sql
CREATE UNIQUE INDEX idx_one_active_run_per_session
ON agent_runs(session_id)
WHERE status IN ('queued', 'starting', 'running', 'waiting_permission', 'compacting', 'stopping');
```

### `events`

Append-only runtime log. This table is the source of truth.

| Column | Type | Notes |
| --- | --- | --- |
| `global_seq` | INTEGER PK AUTOINCREMENT | Canonical global replay cursor |
| `id` | TEXT UNIQUE | Stable event ID for references and idempotency |
| `session_id` | TEXT | Parent session |
| `run_id` | TEXT NULL | Parent run, null for pure system events |
| `task_id` | TEXT NULL | Triggering task |
| `run_seq` | INTEGER NULL | Monotonic within `run_id`; null for pure system events |
| `type` | TEXT | Event type |
| `payload_json` | TEXT | Structured payload |
| `schema_version` | INTEGER | Payload schema version |
| `causation_id` | TEXT NULL | Event or task that caused this event |
| `correlation_id` | TEXT NULL | Groups related events |
| `created_at` | TEXT | Append time |

Important event types:

- `task_created`, `task_started`, `task_finished`, `task_failed`
- `run_queued`, `run_started`, `run_heartbeat`, `run_finished`, `run_failed`
- `text_delta`, `message_completed`
- `tool_call_started`, `tool_call_delta`, `tool_call_finished`
- `permission_prompt`, `permission_resolved`
- `context_budget_changed`, `context_pack_created`
- `delivery_queued`, `delivery_sent`, `delivery_failed`
- `handoff_requested`, `handoff_created`

Recommended indexes:

- `idx_events_session_global_seq` on `(session_id, global_seq)`
- `idx_events_run_seq` unique on `(run_id, run_seq)` where `run_id IS NOT NULL`
- `idx_events_type_global_seq` on `(type, global_seq)`
- `idx_events_correlation`

### `messages`

Projection table for fast conversation reads. Rebuildable from `events`.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | Message ID |
| `session_id` | TEXT | Parent session |
| `run_id` | TEXT NULL | Related run |
| `role` | TEXT | `user`, `assistant`, `system`, `tool` |
| `content` | TEXT | Renderable message content |
| `metadata_json` | TEXT | Source channel, tool name, attachments |
| `source_event_id` | TEXT | Event that produced this row |
| `created_at` | TEXT |  |

### `outbox`

Delivery queue for Web and chat platforms.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | Outbox ID |
| `session_id` | TEXT | Parent session |
| `event_id` | TEXT NULL | Source event |
| `event_global_seq` | INTEGER NULL | Source event cursor |
| `channel_type` | TEXT | `web`, `feishu`, future channels |
| `target_ref` | TEXT | Web connection, Feishu card/message target |
| `kind` | TEXT | `web_event`, `feishu_card_create`, `feishu_card_update`, `feishu_text` |
| `view_model_json` | TEXT | Rendered channel-neutral or channel-specific payload |
| `idempotency_key` | TEXT UNIQUE | Prevents duplicate delivery |
| `status` | TEXT | `pending`, `sending`, `sent`, `failed`, `dead` |
| `attempts` | INTEGER | Retry count |
| `max_attempts` | INTEGER | Usually from config |
| `next_attempt_at` | TEXT NULL | Retry scheduling |
| `locked_by` | TEXT NULL | Worker ID |
| `locked_at` | TEXT NULL | Lease timestamp |
| `lease_expires_at` | TEXT NULL | Lease timeout for crash recovery |
| `provider_message_id` | TEXT NULL | Feishu or channel message ID |
| `last_error` | TEXT NULL | Redacted error |
| `created_at` | TEXT |  |
| `updated_at` | TEXT |  |
| `sent_at` | TEXT NULL |  |

Recommended indexes:

- `idx_outbox_status_next_attempt`
- `idx_outbox_session_created`
- `idx_outbox_locked_at`

### `projector_offsets`

Tracks incremental projections.

| Column | Type | Notes |
| --- | --- | --- |
| `projector_name` | TEXT PK | `messages`, `session_view`, `context_budget`, `feishu_card` |
| `last_global_seq` | INTEGER | Last processed `events.global_seq`; default `0` |
| `last_event_id` | TEXT NULL | Last processed event ID for debugging |
| `updated_at` | TEXT |  |

### `context_packs`

Portable context for compact, resume, and handoff.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | ContextPack ID |
| `session_id` | TEXT | Source session |
| `source_run_id` | TEXT NULL | Source run |
| `schema_version` | INTEGER | ContextPack schema |
| `status` | TEXT | `draft`, `ready`, `failed`, `superseded` |
| `source_event_start_id` | TEXT | Covered event range start |
| `source_event_end_id` | TEXT | Covered event range end |
| `token_estimate` | INTEGER NULL | Estimate for pack |
| `summary_json` | TEXT | Goal, decisions, constraints, current state |
| `recent_messages_json` | TEXT | Recent raw messages or excerpts |
| `key_files_json` | TEXT | Files, commands, artifacts, logs |
| `open_tasks_json` | TEXT | Carry-over work |
| `created_by` | TEXT | `system`, `user`, `agent` |
| `strategy` | TEXT | `native_compact`, `miniagent_summary`, `manual` |
| `created_at` | TEXT |  |

### `schedules`

Cron and one-shot task definitions.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | Schedule ID |
| `session_id` | TEXT | Target session |
| `status` | TEXT | `active`, `paused`, `cancelled` |
| `kind` | TEXT | `once`, `cron` |
| `cron_expr` | TEXT NULL | Cron schedule |
| `run_at` | TEXT NULL | One-shot time |
| `timezone` | TEXT | Default `Asia/Shanghai` |
| `payload_json` | TEXT | Task payload |
| `next_run_at` | TEXT NULL | Scheduler cursor |
| `created_at` | TEXT |  |
| `updated_at` | TEXT |  |

### `audit_logs`

Security and operator trace.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | TEXT PK | Audit ID |
| `actor_type` | TEXT | `web_user`, `feishu_user`, `system`, `agent` |
| `actor_ref` | TEXT NULL | User ID or system component |
| `action` | TEXT | Start, stop, handoff, compact, schedule, secret access failure |
| `resource_type` | TEXT | Session, run, task, schedule, workspace |
| `resource_id` | TEXT NULL | Resource ID |
| `payload_json` | TEXT | Redacted metadata |
| `created_at` | TEXT |  |

## 5. State Machine Rules

### Session

Allowed transitions:

```text
idle -> running
idle -> archived
running -> idle
running -> compacting
running -> failed
compacting -> running
compacting -> idle
compacting -> failed
failed -> idle
failed -> archived
```

Invariants:

- A non-archived session has one fixed `agent_type`.
- MVP allows at most one active `AgentRun` per session.
- Handoff creates a new target session; it does not mutate the source session into another Agent type.

### AgentRun

Allowed transitions:

```text
queued -> starting -> running
running -> waiting_permission -> running
running -> compacting -> stopping -> overflowed
running -> stopping -> succeeded
running -> stopping -> cancelled
running -> failed
starting -> failed
```

Invariants:

- A retry creates a new `AgentRun`.
- Exit code and stop reason are immutable once the run reaches a terminal state.
- Runtime events must be appended before terminal run status is committed.

### Task

Allowed transitions:

```text
scheduled -> queued -> running -> succeeded
scheduled -> paused -> queued
queued -> cancelled
running -> failed
running -> cancelled
```

Invariants:

- A task may create at most one active run at a time.
- Duplicate channel events must be blocked by `dedupe_key`.
- Cron tasks never overwrite an active run; they queue, skip, or fail based on policy.

### Outbox

Allowed transitions:

```text
pending -> sending -> sent
pending -> sending -> failed -> pending
failed -> dead
sending -> pending   # lease timeout recovery
```

Invariants:

- `idempotency_key` is globally unique.
- Delivery failures append events; they do not mutate source events.
- A `dead` item remains queryable and can be manually requeued.
- Workers claim work with a conditional update on status and lease fields; never use read-then-update claiming.

## 6. Transaction Rules

Use a single SQLite transaction for these operations:

- Create task + append `task_created`.
- Start run + update session active run + append `run_started`.
- Append runtime event only; do not render or enqueue Outbox records on the RuntimeSupervisor hot path.
- Project event batch + advance projector offset + enqueue Outbox records derived from projected view changes.
- Finish run + update session + append terminal event.
- Create ContextPack + update session current pack + append `context_pack_created`.
- Handoff source session + target session + task + audit log.

Never emit Web/Feishu side effects before the related database transaction commits.

## 7. SQLite Write, Lease, And Batching Rules

SQLite is acceptable for MVP, but the write path must stay short and predictable.

Required pragmas at startup:

```sql
PRAGMA journal_mode = WAL;
PRAGMA foreign_keys = ON;
PRAGMA busy_timeout = 5000;
PRAGMA synchronous = NORMAL;
```

Write strategy:

- Keep RuntimeSupervisor writes limited to EventStore appends and run/session status transitions.
- Projectors run asynchronously from `events.global_seq` and create messages, context updates, and Outbox entries.
- Use short transactions; never perform network calls or Agent process I/O inside a database transaction.
- If write contention appears, introduce a single writer queue before changing storage engines.

Outbox lease strategy:

- Claim due work with one conditional `UPDATE ... WHERE status IN (...) AND lease_expires_at < now RETURNING ...` style operation, or the SQLite equivalent inside a transaction.
- A worker must refresh or release its lease for long sends.
- Expired `sending` rows return to `pending` and can be retried.
- `dead` rows remain queryable and require manual or explicit system requeue.

Batching strategy:

- RuntimeSupervisor must coalesce raw stdout/stderr chunks before appending `text_delta` events.
- Default batching target: 50-200 ms or a bounded byte size, whichever comes first.
- Store raw chunk-level output only in debug mode; default EventStore rows should be semantically useful deltas.
- Feishu delivery receives a second throttling layer in Delivery, independent of EventStore batching.

## 8. First Migration Order

1. `agent_profiles`
2. `agent_defaults`
3. `sessions`
4. `tasks`
5. `agent_runs`
6. `events`
7. `outbox`
8. `projector_offsets`
9. `messages`
10. `context_packs`
11. `schedules`
12. `audit_logs`

Implement state transition tests before wiring real CLIs.
